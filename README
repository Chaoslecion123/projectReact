npm init -y  // es una pre configuracion del proyecto
1 - se crea la carpeta src
2 - se crea la carpeta public
3 - se crea la carpeta component dentro de src
npm react react-dom
npm install @babel/core babel-loader @babel/preset-env @babel/preset-react --save-dev
npm install webpack webpack-cli html-webpack-plugin html-loader --save-dev
// npm run build  // para que se guarde los cambios en el bundle.js
npm install webpack-dev-server --save-dev
npm install mini-css-extract-plugin css-loader node-sass sass-loader --save-dev
npm install eslint babel-eslint eslint-config-airbnb eslint-plugin-import eslint-plugin-react eslint-plugin-jsx-a11y
npm install file-loader --save-dev
npm install json-server -g
npm install prop-types
npm install react-router-dom --save
npm install redux react-redux --save
npm install md5 --save


*** Redux
// 1 - import connect -- nuestro componente ya esta connectado con redux
// 2 - actions
// 3 - reducers


**** proptypes
// nos ofrece de una manera dinamica de verificar las propiedades que la pasemos a nuestros
// componentes y con esto verifivamos el tipo de datos que lo estamos pasandpo

*** action
// la accion descubre que es lo que vamos hacer
*** reducers
obtiene esta informacion y hace pasar hacia el estado